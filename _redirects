# All redirects are handled by functions/_middleware.js
# Cloudflare Pages strips the domain from same-site _redirects destinations,
# producing relative Location headers that fail GSC validation.
# The middleware uses Response.redirect() which always emits absolute URLs.
